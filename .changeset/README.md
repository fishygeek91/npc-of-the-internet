# Changesets

Agents must run `pnpm changeset` when opening a PR that changes behavior. This records a version bump intent for the release tooling to consume later.

Do not edit `CHANGELOG.md` by hand.
